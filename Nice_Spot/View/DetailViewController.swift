//
//  DetailViewController.swift
//  Nice_Spot
//
//  Created by Ludovic HENRY on 28/02/2021.
//

import UIKit

class DetailViewController: UIViewController {

    override func viewDidLoad() {
        super.viewDidLoad()

        // Do any additional setup after loading the view.
    }

    //    private func loadContent(spot: Spot) {
    //        guard let titleSpot = spot.title else { return }
    //        guard let municipality = spot.municipality else { return }
    //        content = DetailContent(spot: spot)
    //        content?.loadImage(success: { [weak self] success in
    //            guard let self = self else { return }
    //            self.image.image = self.content?.image
    //        })
    //        titleLabel.text = titleSpot
    //        descriptionLabel.text = spot.description
    //        municipalityLabel.text = municipality
    //        titleLabel.text = spot.title
    //    }
    
    
    /*
    // MARK: - Navigation

    // In a storyboard-based application, you will often want to do a little preparation before navigation
    override func prepare(for segue: UIStoryboardSegue, sender: Any?) {
        // Get the new view controller using segue.destination.
        // Pass the selected object to the new view controller.
    }
    */

}
